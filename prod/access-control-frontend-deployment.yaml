apiVersion: apps/v1
kind: Deployment
metadata:
  name: access-control-frontend
spec:
  selector:
    matchLabels:
      app: access-control-frontend
  replicas: 1
  template:
    metadata:
      labels:
        app: access-control-frontend
    spec:
      containers:
      - name: access-control-frontend
        image: danielneset/bachelor-access-control-frontend:prod-web-5bb7474af440ea75c27ea3208cd550aa3d4913f5
        ports:
        - containerPort: 3000
          protocol: TCP
        env:
          - name: AUTH_SECRET
            valueFrom:
              secretKeyRef:
                name: mysql-secret
                key: mysql-root-password
          - name: NEXT_PUBLIC_BACKEND_BASE_URL
            value: "https://admin.flightfinder.space"
          - name: AUTH_TRUST_HOST
            value: "true"
          - name: BACKEND_INTERNAL_URL
            value: "http://access-control-backend-service:8080"